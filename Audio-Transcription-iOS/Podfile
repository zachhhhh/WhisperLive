# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'WhisperLive-iOS-Client' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WhisperLive-iOS-Client

  # Monetization: Google Mobile Ads
  pod 'Google-Mobile-Ads-SDK', '11.8.0'

  # IAP is built-in with StoreKit, no pod needed

  target 'WhisperLive-iOS-ClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
    end
  end
end